class Code4me2CoreCommunicator {
    private val modules = mutableListOf<ModuleInterface>()

    // Register a module
    fun registerModule(module: ModuleInterface) {
        modules.add(module)
        println("Module registered: ${module.moduleName}")
    }

    // Process data from a module
    fun processData(module: ModuleInterface, data: Map<String, Any>) {
        println("Processing data from ${module.moduleName}: $data")
        // Forward data to the core platform (implementation omitted)
    }
}
